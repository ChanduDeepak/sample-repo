name: CI Workflow

on: 
  push:
    branches:
      - main
jobs:
  Build-test-job:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout code
        uses: actions/checkout@v4
      - name: Run tests
        run: echo "Running test jobs" #testing once
        
        
    
